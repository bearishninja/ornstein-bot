state.json
.env
__pycache__/
*.pyc
# Keep the droplet guardrail (hook + registration) in version control —
# it must survive a fresh clone. Only the machine-local permissions file is
# ignored.
.claude/settings.local.json
